name: GitHubCI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build all the Docker images
        run: docker-compose build --compress --parallel
